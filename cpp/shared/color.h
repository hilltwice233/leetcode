#ifndef __color_h
#define __color_h

#include <string>

using namespace std;

string dim(string raw);
string red(string raw);
string yellow(string raw);
string green(string raw);
string cyan(string raw);
string blue(string raw);
string magenta(string raw);

#endif
